"""сначала прочитайте весь файл а потом следуйте инструкцмям, я вносила много правок и изменений,и всё написала в файле, так что только после полного прочтения, вы можете вернуться к определенному блоку в тексте и вводить скрипт дальше по плану, это дает вам выбор в упрощении запуска проекта , это те фазы, которые вы можете так же сделать по своему, как вам будет проще.

"""


"""
я также опишу все проблемы с установкой с которыми я столкнулась, и если вдруг у вас будет одна или несколько из них - вы будете знатьчто делать
"""


"""
если и после этого ничего не работает, то установите себе Anaconda Prompt и не морочьте себе голову. Смотря что вкуснее как говориться)
"""



"""
введите   conda init , потом для активации conda activate ""music-env""- назвать можете как угодно будет вам. потом команда conda create -n music-env python=3.13   --версию python выберите ту на которой работаете, у меня python 3.13. и после запуска в терминале у вас должно появиться имя вашего окружения (music-env)   И ФОТО ВСТАВИТЬ С ИНЕТА
"""




"""
установите в окружении: conda install -c conda-forge music21 

wkhtmltopdf

pip install pdfkit pillow, pip install opencv-python,, сначала установите Cython а потом уже omnizart - и тогда все будет загружаться хорошо. а если все равно будет ошибка, тогда введите -   pip install --ignore-installed omnizart.
, flask, flask_cors, pillow, pdf2image,pytesseract,bcrypt. Flask-сервер(backend) должен запускаться в активном окружении Conda, потому что он использует библиотеки, установленные в этом окружении (music21, pytesseract и так далее) а вот сам react(frontend), он  не требует активного окружения Conda, потому что он использует Node.js и npm для запуска. и поэтому вы можете запускать его в обычном терминале.
"""
есть файл:environment.yml  - там все зависимости. запустите его командой: conda env create -f environment.yml 


"""

Flask-сервер зависит от библиотек Python, установленных в окружении Conda. Если вы запустите его без активации окружения, Python не сможет найти эти библиотеки.
React-приложение использует Node.js и npm, которые не зависят от Conda. Поэтому его можно запускать в любом терминале.
4. Как упростить запуск?
Если вы часто запускаете проект, можно создать два отдельных скрипта:

Для Flask-сервера:
Создайте файл start_flask.bat в папке backend:
@echo off 
call conda activate music-env
python app.py
Запустите его двойным кликом.

Для React-приложения:
Создайте файл start_react.bat в папке frontend:


@echo off
npm start
Запустите его двойным кликом. и почему .bat  расширение??"""